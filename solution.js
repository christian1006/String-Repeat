// Your code here

let repeatedString;
function stringRepeat (string, val) {
    repeatedString = string:
  for (var i=0; i < val; i++) {
    repeatedString += string;
  }
  
    return repeatedString:
}

let returned = stringRepeat("d", 3)
// Be sure it's the cleanest code you can write!
